---
description: 代码质量与复杂度规范
alwaysApply: true
---

# 代码质量与复杂度规范

本项目使用复杂度检测规则，确保代码保持可维护性。

## 文件大小限制

| 文件类型 | 警告 | 错误 |
|----------|------|------|
| .ts 文件 | 200 行 | 300 行 |
| .tsx 文件 | 150 行 | 200 行 |

超出限制时应拆分组件或提取逻辑到单独模块。

## 函数复杂度

### 嵌套深度

| 级别 | 最大嵌套 |
|------|----------|
| 警告 | 3 层 |
| 错误 | 4 层 |

```typescript
// 禁止 - 超过 4 层嵌套
if (a) {
  if (b) {
    if (c) {
      if (d) {
        // 太深了
      }
    }
  }
}

// 推荐 - 使用卫语句或提取函数
if (!a || !b || !c || !d) return;
process();

// 或提取为函数
const shouldProcess = (a: boolean, b: boolean, c: boolean, d: boolean) => a && b && c && d;
if (shouldProcess(a, b, c, d)) {
  // ...
}
```

### 函数长度

| 类型 | 警告 | 错误 |
|------|------|------|
| 普通函数 | 30 行 | 50 行 |
| React 组件 | 50 行 | 80 行 |

### 参数数量

| 级别 | 最大参数 |
|------|----------|
| 警告 | 4 个 |
| 错误 | 6 个 |

```typescript
// 禁止 - 参数过多
function createStrategy(name, desc, author, tags, config, initialCapital) { ... }

// 推荐 - 使用配置对象
function createStrategy(options: {
  name: string;
  description: string;
  author: string;
  tags: string[];
  config: StrategyConfig;
  initialCapital: number;
}) { ... }
```

## 重复代码检测

当发现 3 处以上相同模式（每处 10+ 行）时，应提取为：
- 共享组件
- 自定义 Hook
- 工具函数

```typescript
// 重复代码示例 - 应提取
// 三处都有相同的卡片样式逻辑

// 提取前
<Card style={{ bg: '#141414', border: '#2a2a2a' }} />
<Card style={{ bg: '#141414', border: '#2a2a2a' }} />
<Card style={{ bg: '#141414', border: '#2a2a2a' }} />

// 提取后 - 使用预设
import { CardStyles } from '@/presets/presets';

<Card style={{ bg: CardStyles.default.bg, border: CardStyles.default.border }} />
```

## Import 数量限制

| 类型 | 最大数量 |
|------|----------|
| 每个文件的 import | 15 个 |
| 每个文件的 export | 10 个 |

## 代码味道警告

| 问题 | 检测 | 处理建议 |
|------|------|----------|
| 巨型组件 | >150 行 | 拆分 |
| 深层嵌套 | >3 层 | 提取函数/卫语句 |
| 重复代码 | >3 处 | 提取为组件/Hook |
| 魔法数字 | 任何 | 提取为常量 |
| 过长函数 | >50 行 | 拆分 |
| 参数过多 | >6 个 | 使用配置对象 |

## 注释规范

### 需要注释的情况

1. 复杂的业务逻辑
2. 非显而易见的算法
3. 外部依赖的行为
4. TODO 和 FIXME

```typescript
// 需要注释 - 复杂的金融计算
// 夏普比率 = (平均收益率 - 无风险利率) / 收益率标准差
const sharpeRatio = (avgReturn - riskFreeRate) / stdDev;

// 需要注释 - 外部依赖的行为
// 注意：API 返回的时间戳是毫秒级 Unix 时间
const timestamp = response.data.timestamp * 1000;
```

### 不需要注释的情况

```typescript
// 不需要注释 - 代码本身已经很清楚
// 设置加载状态
setLoading(true);

// 更好的写法 - 直接表达意图
setLoading(true);
```

## 魔法数字

所有硬编码的数字应提取为命名常量：

```typescript
// 禁止 - 魔法数字
if (user.score > 100) { ... }
setTimeout(() => {}, 3000);
const maxRetries = 3;

// 推荐 - 有名称的常量
const MAX_ALLOWED_SCORE = 100;
const RETRY_DELAY_MS = 3000;
const MAX_RETRY_COUNT = 3;

if (user.score > MAX_ALLOWED_SCORE) { ... }
setTimeout(() => {}, RETRY_DELAY_MS);
```

## 复杂条件简化

### 卫语句模式

```typescript
// 嵌套的 if-else
if (isValid) {
  if (hasPermission) {
    if (!isLocked) {
      // 执行逻辑
    }
  }
}

// 使用卫语句
if (!isValid) return;
if (!hasPermission) return;
if (isLocked) return;
// 执行逻辑
```

### 提取复杂条件

```typescript
// 复杂的条件判断
if (user.role === 'admin' && user.active && !user.banned && user.permissions.includes('write')) {
  // ...
}

// 提取为函数
const canWrite = (user: User) => 
  user.role === 'admin' && user.active && !user.banned && user.permissions.includes('write');

if (canWrite(user)) {
  // ...
}
```

## 代码审查检查清单

在提交代码前检查：

1. 文件是否超过 200 行（.tsx）或 300 行（.ts）？
2. 函数是否超过 50 行？
3. 嵌套深度是否超过 4 层？
4. 是否有重复代码可以提取？
5. 是否有魔法数字需要提取为常量？
6. 所有 props 是否都有类型定义？
7. 是否正确使用了预设的颜色和样式？

## 配置

这些规则在 `src/rules/refactor-rules.json` 中配置：

```json
{
  "rules": {
    "fileLines": { "warning": 200, "error": 300 },
    "functionLines": { "warning": 50, "error": 80 },
    "nestingDepth": { "warning": 3, "error": 4 },
    "maxParams": { "warning": 4, "error": 6 }
  }
}
```
